#pragma once

#include "AComponent.hpp"

namespace potEngine
{
    class MonstreComponent : public AComponent {
    public:
        MonstreComponent() = default;
        ~MonstreComponent() = default;
    };
}
